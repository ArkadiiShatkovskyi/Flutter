import 'package:flutter/material.dart';

class ExpensesHistory extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Expenses history"),
    );
  }
}